Set up the GitHub repo today and got the basic project structure in place for both client and server sides. Added a .gitignore so node_modules and .env files don't get pushed. Got Express installed and wrote the initial server.js file. On the React side, created the public HTML file and the root index.js to get the app rendering.



Version control is set up using Git where each feature is tracked through commits. The MERN stack separates the application into a React frontend that runs on the browser and a Node/Express backend that handles data and logic. The project follows a client-server architecture where the two communicate over HTTP. Express is a minimal Node.js framework used to define routes and handle requests.